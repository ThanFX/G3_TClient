{
    "version": "2.0.0",
    "cwd": "${workspaceFolder}",
    "tasks": [
        {
            "label": "Build and run",
            "type": "shell",
            "linux": {
                "command": "go build && ./G3_TClient"
            },
            "windows": {
                "command": "go build; ./G3_TClient.exe"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}